import MediaCard from "./muiCard";

const Projects = () => {
    return (
        <div className="projects">
            <h2>Projects</h2>
            <MediaCard />
        </div>
    );
};

export default Projects;
